<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.philoertel.juryduty"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".JuryDutyApplication">
        <activity android:name=".DutiesActivity">
        </activity>
        <activity
            android:name=".DutyActivity"
            android:label="@string/title_activity_duty"
            android:parentActivityName=".DutiesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.philoertel.juryduty.DutiesActivity"/>
        </activity>
        <activity
            android:name=".AddDutyActivity"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.philoertel.juryduty.MainActivity"/>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".DebugActivity">
        </activity>

        <receiver android:name=".BootCompletedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".CheckInAlarmReceiver">
        </receiver>
    </application>
</manifest>